# NetworkService

Service-layer wrapper over network access.

## Responsibilities

- Creates `IHTTPClient` instances on demand, delegating to `IFactory`

## Key Classes

- `NetworkService` - Main implementation of `INetworkService`
