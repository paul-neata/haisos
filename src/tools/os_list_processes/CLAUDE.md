# os_list_processes

Lists the OS's currently running processes.

## Arguments

None.

## Output format

On success, returns a JSON array of `{"pid": ..., "parent_pid": ..., "name": ..., "finished": ...}` objects.
