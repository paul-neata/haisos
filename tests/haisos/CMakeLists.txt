# Haisos tests are JS-based tests that run against the haisos engine.
# This directory contains subdirectories with .haisostest suffix.
# Each subdirectory contains .haisostest.js files run with Node.js.
